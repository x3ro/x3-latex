\documentclass[
    a4paper,
    twoside,
    11pt
]{article}

%regular letter size is 210 wide by 297 long
\usepackage[
    paperwidth=270mm,
    paperheight=297mm,
    hmarginratio=1:1,
    top=25mm,
    bottom=25mm,
    columnsep=20pt,
    footnotesep=1cm,
    left=55mm
]{geometry}

\input{x3-general-preamble}

% Todo-notes using \todo
% Needs to be included here because textwidth is different for the
% normal preamble version
\usepackage[textwidth=50mm]{todonotes}
\setlength{\marginparwidth}{50mm} % Fixes left column todos overlapping the column.

% Add switch to disable the Abstract
\newif\ifDisableAbstract

% Disable roman numbers for section numbering
\newif\ifDisableRomanSections

% Single column switch
\newif\ifSingleColumn

% Single column switch
\newif\ifSingleColumnTOC

